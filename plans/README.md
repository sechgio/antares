# Implementation Plans

Generated by the improve skill.

- **Pass A (FE perf / safety)** — 2026-07-13 @ `1395878` — plans **001–006** (DONE).
- **Pass B (backend Python deep audit)** — 2026-07-13 @ `1395878` — plans **007–017** (TODO).

## Global hard rule (every plan)

**Preserve user-visible functionality.** Prefer additive guards, characterization tests first, and default-path parity.

Do **not** change without an explicit STOP + human decision:

- Conversion / rename **on-disk outputs** (bytes, filenames, mapping hits)
- Sellador stamp placement accuracy
- PDF/DOCX visual content for normal templates
- History semantics (except where a plan narrowly documents additive fields)
- IPC field removals that FE still reads

Each plan has a **Functional invariants** table — treat it as part of done criteria.

Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully, honor STOP conditions, update your row when done.

---

## Execution order & status

### Pass A — Frontend performance (DONE)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden conversion process runner state | P1 | S | — | DONE — PR https://github.com/sechgio/antares/pull/47 (@ 23a97d6) |
| 002 | Keep backend alive during long single-file jobs | P1 | M | — | DONE — PR https://github.com/sechgio/antares/pull/48 (@ 93a8326) |
| 003 | Single-flight rename/image previews without double probe | P1 | M | — | DONE — PR https://github.com/sechgio/antares/pull/49 (@ 98c15db) |
| 004 | Cap Sellador preview raster + bound caches | P2 | M | — | DONE — PR https://github.com/sechgio/antares/pull/50 (@ 6389fea) |
| 005 | Display-size conversion thumbnails with full-res fallback | P2 | M | — | DONE — PR https://github.com/sechgio/antares/pull/51 (@ 89add0a) |
| 006 | Low-risk FE perf quick wins (Espacios/History/AutoIMG) | P2 | S | — | DONE — PR https://github.com/sechgio/antares/pull/52 (@ 04588f8) |

### Pass B — Backend Python (TODO)

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 007 | Always emit complete/error when conversion job dies | P1 | S | — | DONE (Wave 1 local) |
| 008 | Run `process_start` on light lane (not heavy) | P1 | S | — | DONE (Wave 1 local) |
| 009 | Slim `process_status` (omit bulk `files` list) | P1 | S | — | DONE (Wave 2 local) |
| 010 | Redact map API keys from ubicaciones logs | P1 | S | — | DONE (Wave 1 local) |
| 011 | Preview ≡ process key-column resolution (process-preserving) | P1 | S–M | — | DONE (Wave 3 local) |
| 012 | Atomic fields/patterns config JSON writes | P2 | S | — | DONE (Wave 1 local) |
| 013 | Bundle pymupdf/fitz in PyInstaller | P1 | S | — | DONE (Wave 1 local) |
| 014 | Characterization tests: plugin AST + formatos PDF | P1 | M | — | DONE (Wave 1 local) |
| 015 | Cap inbound IPC payload size | P2 | S | — | DONE (Wave 1 local) |
| 016 | Sanitize all WeasyPrint HTML→PDF paths | P2 | S–M | — | DONE (Wave 4 local) |
| 017 | Mapping orphan stats O(1) with result parity | P2 | S | — | DONE (Wave 1 local) |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

---

## Recommended execution order (Pass B)

```
Wave 1 (parallel, zero product-behavior risk):
  007 complete-on-error
  008 process_start light
  010 redact map keys
  012 atomic config
  013 PyInstaller fitz
  014 characterization tests
  015 inbound IPC cap
  017 mapping orphans O(1)

Wave 2 (IPC shape / careful):
  009 slim process_status   ← confirm FE does not read params.files first

Wave 3 (behavior-alignment; process disk outputs must stay same):
  011 preview/process key parity

Wave 4 (PDF security; needs WeasyPrint env for full verify):
  016 WeasyPrint sanitize parity
```

### Dependency notes

- **007–010, 012–015, 017** are independent of each other.
- **009** is independent but must not drop `running`/`progress`/`logs`.
- **011** must **not** change process rename-on-disk for empty `key_column` (align preview → process).
- **014** has no code dependency but should land before any future plugin-sandbox *tightening* or formatos refactors (not planned here).
- **016** reuses the pattern already in `render_pdf_html`; keep data-URI images working.
- **013** is packaging-only; unfrozen tests alone are insufficient for full proof — note freeze smoke in PR if possible.

---

## Non-regression checklist (all plans)

Before marking DONE:

1. Existing related tests still pass.
2. New characterization / regression tests cover the failure mode.
3. Functional invariants table in the plan still holds.
4. No unrelated refactors; scope limited to listed files.
5. Backend: `python -m ruff check <touched>` and plan-specific pytest green.
6. If FE types touched: `npm run typecheck:frontend`.
7. Prefer `python -m pytest tests/<focus> -v` over full `npm test` during iteration; run broader suite before PR.

---

## Findings considered and rejected / deferred (Pass B)

| Item | Verdict |
|------|---------|
| Cancel drain / join heavy workers after cancel | Deferred — changes cancel latency UX; existing test encodes “don’t wait for slow workers”. Needs dedicated design (draining state). |
| Atomic conversion image writes (`tmp`+replace) | Deferred — Windows file-lock risk; higher blast radius than config atomic writes. |
| Plugin sandbox allowlist rewrite | Deferred after **014** characterization; product “use at own risk” today. |
| Drop pandas / unify pypdf+pymupdf | Not worth it now; dual PDF stack intentional. |
| Full `ubicaciones.py` split | Documented stay-as-is; tests patch module globals. |
| Direction D1–D5 (historial hub, fichas export, …) | Product options — not implementation plans this pass. |
| Mid-flight IPC retry of mutators | Still deferred (prior pass). |
| Absolute IPC paths as “vuln” | By design local desktop tool. |
| Lowering IPC max below 64 MiB | Forbidden without product OK (**015** keeps default). |

### Pass A rejections (historical)

- Mid-flight IPC retry of non-idempotent mutators
- Unify FE + main timeout ownership globally
- Image optimizer off-main-thread
- Formatos multi-page virtualization
- Tab keep-alive product trade-off

---

## Plan → audit finding map (Pass B)

| Plan | Audit IDs |
|------|-----------|
| 007 | CORRECTNESS-02 |
| 008 | CORRECTNESS-04 / PERF-12 |
| 009 | PERF-04 |
| 010 | SECURITY-02 |
| 011 | CORRECTNESS-03 |
| 012 | CORRECTNESS-09 |
| 013 | DEPS-01 |
| 014 | TESTS-01 / TESTS-02 / SECURITY-01 (tests only) |
| 015 | SECURITY-04 |
| 016 | SECURITY-03 |
| 017 | PERF-06 |
