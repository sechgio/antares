{
  "name": "antares-app",
  "version": "0.10.13",
  "description": "Antares",
  "author": "Antares <dev@antares.app>",
  "private": true,
  "main": "electron/main.js",
  "homepage": ".",
  "repository": {
    "type": "git",
    "url": "https://github.com/sechgio/antares.git"
  },
  "bugs": {
    "url": "https://github.com/sechgio/antares/issues"
  },
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"wait-on http://localhost:5173 && electron electron/main.js\"",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "node scripts/build-backend.js",
    "clean:dist-electron": "node scripts/clean-dist-electron.js",
    "clean:after-package": "node scripts/clean-after-package.js",
    "build:win": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder --win && npm run clean:after-package",
    "build:mac": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder --mac && npm run clean:after-package",
    "build:linux": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder --linux && npm run clean:after-package",
    "build:all": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder --win --mac --linux && npm run clean:after-package",
    "dist": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder && npm run clean:after-package",
    "dist:dir": "npm run build:backend && npm run build:frontend && npm run clean:dist-electron && electron-builder --dir",
    "test": "node tests/test-version-sync.js && node tests/test-push-loop.js && node tests/test-pr-fix-loop.js && cd backend && python -m pytest ../tests -v && node ../tests/test-electron-path.js && node ../tests/test-electron-ipc-allowlist.js && node ../tests/test-html-sanitizer.js && node ../tests/test-backend-spawner.js && node ../tests/test-backend-spawner-handshake.js && node ../tests/test-backend-spawner-recovery.js && node ../tests/test-backend-spawner-midflight-exit.js && node ../tests/test-backend-spawner-health.js && node ../tests/test-backend-spawner-shutdown.js && node ../tests/test-electron-dialogs.js && node ../tests/test-electron-preload.js && node ../tests/test-build-size-guards.js && cd ../frontend && npx vitest run",
    "test:frontend": "cd frontend && npx vitest run",
    "test:stress": "cd backend && python -m pytest ../tests/test_stress_conversion.py -v -m slow",
    "lint:python": "ruff check backend tests scripts",
    "lint:fix": "ruff check backend tests scripts --fix",
    "typecheck:backend": "python -m mypy backend",
    "typecheck:frontend": "cd frontend && npx tsc --noEmit",
    "audit:python": "pip-audit .",
    "ci": "npm run lint:python && npm run typecheck:backend && npm run audit:python && npm run typecheck:frontend && npm test",
    "bump:patch": "node scripts/bump-version.js patch",
    "bump:minor": "node scripts/bump-version.js minor",
    "bump:major": "node scripts/bump-version.js major",
    "release:dry-run": "node scripts/release-loop.js",
    "release:ship": "node scripts/release-loop.js --ship",
    "release:full": "node scripts/release-loop.js --ship --build",
    "push:dry-run": "node scripts/push-loop.js",
    "push:ship": "node scripts/push-loop.js --ship",
    "push:merge": "node scripts/push-loop.js --ship --merge",
    "pr-fix": "node scripts/pr-fix-loop.js",
    "pr-fix:ship": "node scripts/pr-fix-loop.js --ship",
    "pr-fix:merge": "node scripts/pr-fix-loop.js --ship --merge",
    "hooks:install": "node scripts/install-hooks.js",
    "postinstall": "electron-builder install-app-deps"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0",
    "wait-on": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  }
}
